[project]
name = "skillnet-api"
version = "0.1.0"
description = "SkillNet FastAPI backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg",
    "greenlet",
    "alembic",
    "pgvector",
    "pydantic>=2",
    "pydantic-settings",
    "fastapi-users[sqlalchemy]",
    "python-multipart",
    "litellm<1.92",
    "langgraph",
    "tiktoken",
    "pypdf",
    "python-docx",
    "tenacity",
    "httpx",
    # Direct import in src/core/secrets.py (Fernet), which encrypts the provider API key
    # an organization stores. Already present transitively; declared because we use it.
    "cryptography",
]

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "aiosqlite",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = ["integration"]
