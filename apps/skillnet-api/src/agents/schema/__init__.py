"""Design-time schema proposal pipeline (LangGraph, v2).

Proposes the node graph of a course from its source document. It writes **zero**
content: content is generated per learner at runtime by ``src.agents.runtime``.
"""
